# Status and diff
alias gs='git status'
alias gd='git diff'

# Log
alias gl='git log -1'
alias gll='git log -3'
alias glog='git log'

# Add and commit
alias ga='git add'
alias gall='git add --all .'
alias gc='git commit'
alias gca='git add --all .; git commit'

# Pull and push
alias gp='git pull && git push' # Won't push if fails to push

# Reset
alias grh='git reset --hard HEAD'
